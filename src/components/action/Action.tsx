import React from 'react';
import './Action.css';
import envelope from '../../assets/envelope-regular.svg';

const Action: React.FC = () => {
    return (
        <div id='action '>
            <img src={envelope} alt='eye' />
        </div>
    );
};

export default Action;